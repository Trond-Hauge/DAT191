"use strict";

// Should validate password according to set requirements.
export function validatePassword(pass) {
    return false;
}

// Needs to create a secure representation of the password to be stored in database.
export function securePassword(pass) {
    return pass;
}